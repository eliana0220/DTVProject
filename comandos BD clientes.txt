  CREATE TABLE TVCHANNELS (
  codTVCHANNEL INTEGER NOT NULL PRIMARY KEY,
  nameTVCHANNELS VaRCHAR(20),
  pack VaRCHAR(20));

  CREATE TABLE TVCLIENTS (
  dniClient INTEGER NOT NULL PRIMARY KEY,
  nameTVCLIENTS VaRCHAR(20));
  
    CREATE TABLE TVCLIPACK (
    dniClientCLIPACK INTEGER NOT NULL PRIMARY KEY,
     codCHANNELclipack INTEGER NOT NULL);  
     
  CREATE TABLE TVPACKS (
   codTVPACKS INTEGER NOT NULL PRIMARY KEY,
   nameTVPACKS VaRCHAR(20));
   
   -----------------------
   
   CREATE TABLE MAILS (
    nameMAIL VARCHAR(20) NOT NULL PRIMARY KEY,
     mailMAILS VARCHAR(20)); 
     
  CREATE TABLE NUMBERS (
    nameNUMBER VARCHAR(20) NOT NULL PRIMARY KEY,
     numNUMBER VARCHAR(20));
       
CREATE TABLE MESSAGES (
    rem VARCHAR(20) NOT NULL PRIMARY KEY,
    dest VARCHAR(20),
    message VARCHAR(20));
    
  --------------------------
    CREATE TABLE MOVIEPACKS (
   codMOVIEPACKS INTEGER NOT NULL PRIMARY KEY,
   nameMOVIEPACKS VARCHAR(20));
   
   CREATE TABLE MOVIES (
   codMOVIES INTEGER NOT NULL PRIMARY KEY,
   nameMOVIES VARCHAR(20),
   packMOVIES VARCHAR(20)); 
   
     CREATE TABLE MOVIECLIENTS (
  dniMOVIECLIENT INTEGER NOT NULL PRIMARY KEY,
  nameMOVIECLIENT VaRCHAR(20));
  
  CREATE TABLE MOVIECLIPACK (
    dniClientMOVIECLIPACK INTEGER NOT NULL PRIMARY KEY,
     codMOVIECLIPACK INTEGER NOT NULL);  
     
 ------------------------------
  
    CREATE TABLE INTCLIENTS (
  dniINTCLIENTS INTEGER NOT NULL PRIMARY KEY,
  nameINTCLIENTS VaRCHAR(20));
  
  CREATE TABLE INTCLIPACK (
    dniINTCLIPACK INTEGER NOT NULL PRIMARY KEY,
     codINTCLIPACK INTEGER NOT NULL);   
     
  CREATE TABLE INTPACK (
   codINTPACK INTEGER NOT NULL PRIMARY KEY,
   nameINTPACK VARCHAR(20));   
   
   
   
   
   
   
   
   
  
         
     
    
  
  
  
 
